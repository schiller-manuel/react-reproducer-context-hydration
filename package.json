{
  "name": "react-dehydrated-suspense-context-repro",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "start": "node build.mjs && node dist/server.js",
    "dev": "node build.mjs && node dist/server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
